services:
  frontend_dev:
    container_name: nexus_frontend_dev
    image: docker.io/${DOCKER_USERNAME?error}/${PROJECT_NAME?error}:dev
    restart: always
    ports:
      - '3001:3000'
    env_file:
      - .env